$c[Supports Slash Commands! Requires Text Option "prompt"]

$nomention
$if[$isSlash==false]
$argsCheck[>1;❌]
$endif
$reply
$if[$isSlash==false]
$httpGet[https://text.pollinations.ai/$url[encode;$message]]
$else
$httpGet[https://text.pollinations.ai/$url[encode;$message[prompt]]]
$endif
$onlyIf[$httpStatus!=400;❌ The AI Doesn't Want To Answer.
This can be because your message Is Inappropiate or There was an error.]
$onlyIf[$httpStatus!=502;❌ Try Again!]
$httpResult
